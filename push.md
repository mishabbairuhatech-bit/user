git push https://mishabbairuhatech-bit@github.com/mishabbairuhatech-bit/user.git

ngrok http 3000

1. cd server && npm run sync:billing — creates tables + seeds data
2. Re-run the role-permission seed to add new permissions
3. cd server && npm run start:dev — start backend
4. cd admin && npm run dev — start frontend